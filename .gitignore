# Generated by any project under projects/ (Vite, npm). Patterns without a
# leading slash match at every depth, so projects/website/node_modules/ and
# projects/website/dist/ are covered, and so will the next project's be.
node_modules/
dist/

# Operating-system noise and local archives.
.DS_Store
Thumbs.db
*.zip
