# Vagora website — project record

This file is the durable record of the v6 rebuild: what exists, why, what is frozen, what is pending. Read this first in any new session. `DIRECTION.md` holds the art direction; `README.md` holds run instructions.

## 1. What the project is

A single-page marketing site for Vagora, an in-store 3D try-on mirror sold to retailers. Audience: founders and merchandising directors. Lives in `D:\website-vskill` as a Vite project.

```
index.html               the page (all copy lives here)
src/main.js              behaviour: Lenis + GSAP ScrollTrigger, intro, reveals, floor state machine, Act two ledger state machine, dock spy, cursor, copy button
src/modules/split.js     accessible word/line splitter (keeps an unsplit visually-hidden name; honours authored <br>). Each `.split-line` is the reveal mask with a glyph safety area (0.1em top / 0.16em bottom / 0.07em inline, margins compensating) and opens to `overflow: visible` once the heading is in (or motion is reduced), so no resting glyph is ever cropped
src/styles/main.css      foundations (tokens) + every chapter's styles, in page order
assets/                  brand, fonts (Switzer + FFL licence; Fragment Mono + OFL), imagery (concept renders), intro film — Vite bundles what is referenced
public/                  robots.txt, share-card.jpg (files that must keep their exact path)
vite.config.js           base "./" so dist/ deploys from any path; gsap+lenis in one chunk
dist/                    build output (ignored by git)
```

Run: `npm install`, `npm run dev`, `npm run build` (→ `dist/`), `npm run preview`.

Narrative: ACT ONE · ON THE FLOOR (what the shopper experiences) → THE VAGORA MIRROR (the physical product connecting experience and business; not an act) → ACT TWO · IN THE BUSINESS (what the retailer gets back). Tonal progression: white → `#0F0F0F` (the Mirror chapter) → pure `#000000` reserved for the closing chapter. Product noun: “the Vagora Mirror” — never unit, kiosk, terminal, station.

Legacy from v5 still in the folder and **not used by the build**: `tokens/`, `reference/` (internal reference surfaces that need `tokens/`), and `src/modules/hero-gl.js` (dead file from the removed WebGL hero — delete it). `assets/` is shared and stays.

## 2. Evidence rules (do not break)

- No customer, store, pilot result, uplift figure, testimonial, avatar or logo wall appears anywhere: none is evidenced yet.
- The console panel shows sample data and is labelled "Illustrative" on the asset and "Sample data, not a live estate." beneath it; its accessible name says so too.
- Imagery is concept renders of the Mirror (noted in a source comment, not on the page — the footer line was removed by request). `mirror-screen-scene.jpg` and `vagora-mirrror.jpeg` are no longer referenced and can be deleted.
- Copy is the client's; product claims are not to be rewritten or invented.
- Icons: Solar (CC BY 4.0) inlined as SVG symbols. Fonts: Switzer (Fontshare FFL) for everything Vagora says; Fragment Mono regular (OFL 1.1, self-hosted latin woff2 from the `@fontsource/fragment-mono` 5.3.0 package, licence in `assets/fonts/FragmentMono-OFL.txt`) for the micro language only.
- Before launch: `og:image` and `rel="canonical"` must become absolute URLs (marked in `index.html`).

## 3. Foundations (approved, refinement 02)

All in `:root` at the top of `main.css`. No second system exists; every component references these.

- **Colour** — monochrome. Light: `#FFFFFF` surface (`#F7F7F6` `--surface-object` / `data-surface="object"` exists as a token but is currently unused — the Mirror chapter moved to `#0F0F0F`), `#0F0F0F` ink, secondary 62%, muted 45%, rule 10%. Dark: `#000000`, white, 68%, 45%, 14%. Every other tone (frost, media ground, control hover, dock wash) is an alpha of ink or white. Status hues exist only for the console pills. `[data-surface="dark"]` re-resolves the aliases (`--bg --text --text-2 --text-3 --line --control-*`) and steps weights down one.
- **Type** — Two voices. **Fragment Mono** (`--mono`, regular only) is the micro language: one 14px tier, weight 400, 0.055em tracking on uppercase labels / 0.045em on indexes, applied to every `.eyebrow` and, via `.notation`, to indexes, RETAIL IMPACT labels, specification and FAQ indexes and console metadata. **Switzer** carries everything else. Headline roles, largest to smallest — the visual object in each composition decides how loud the type is, not the HTML level: **display** `clamp(3.75rem, 1.5rem + 7.5vw, 10rem)` (160 max — the closing “See it in your store.” and the curtain count); **lead** `clamp(2.625rem, 1.5rem + 3.33vw, 5.25rem)` (84 max — the manifesto proposition); **chapter** `clamp(2.25rem, 1.4rem + 2.6vw, 4rem)` (64 max — the two act openings, `.floor__title` / `.business__title`); **section** `clamp(1.75rem, 1.2rem + 1.6vw, 3rem)` (48 max — `.h2`/`.h3`: the Mirror, the Console, Before you commit). Then **feature** ~22px (body tier, weight 500: act feature titles and FAQ questions), **copy** ~17px (small tier: descriptions, impact sentences, the Mirror paragraph, FAQ answers, the console paragraph) and **micro** 14px mono. Body tier 18→22 and small 16→17 are unchanged. Weights 500 light / 450 dark; body 400.
- **Gutter** — `--page-x: clamp(1.5rem, 1rem + 1.1vw, 3rem)` (32px @1440, 38 @2000, 48 max). No global max-width anywhere; reading limits `--reading-max 42rem` / `--copy-max 30rem` apply to copy only.
- **Space** — 8·12·16·24·32·48·64·96·128·160 (`--space-1..10`); section rhythms sm/md/lg.
- **Geometry** — `--radius-media: 0`, `--radius-control: 8px`, `--radius-pill: 999px`. Device frame keeps its hardware radii (14/9px).
- **Motion** — 180 / 320 / 700ms, `cubic-bezier(0.22, 1, 0.36, 1)`. Signature choreography (reveal 900ms, dock arrival 480ms, dock flip 520ms) keeps authored timings. `prefers-reduced-motion`: no Lenis, no curtain, no scrub, poster instead of film, instant states.

## 4. Chapters (page order) and their status

1. **Intro / hero** — black, the film full-viewport with nothing written over it; poster is the static frame; pause/play toggle top-right. JS intro: black curtain with mark + 00→100 counter → curtain lifts, film settles (scale 1.08→1) → dock rises. Frozen.
2. **Manifesto** — white; word-by-word opacity scrub of the definition sentence; three meta items. Frozen.
3. **Act one · On the floor (Experience)** — see §5. Composition approved; transitions pending.
4. **The Vagora Mirror** — dark sculptural product chapter on `#0F0F0F` (`data-surface="dark"` with `--bg: var(--ink)`; pure black stays reserved for the closing chapter). Text white / 68% / 45%, rules 14%; photography supplies the colour. Local stage `min(100% − 2·--page-x, 1680px)`, centred. **Wide (≥1200px):** grid `55fr | 37fr`, gap `clamp(48px, 5vw, 96px)`; left, the product field — `assets/imagery/vagora-mirror-dark.jpeg` (4:5, temporary) height-driven at `--media-h: clamp(560px, 80svh, 1040px)`, centred in its field (left of the chapter's centre, clear of the page edge), its outer environment softened into the surface by a purely functional edge mask (sides 0→26% / 82→100%, top 6%, bottom 90→100%; the product sits in the untouched centre — frame, sensor arm, base and casters all visible); right, `THE VAGORA MIRROR` label, section-tier headline (two lines, `max-width: 21ch`), paragraph "Freestanding on the shop floor…" at body tier, 68% white, `36ch`; the annotation begins at 26% of the product's height. Beneath both: the technical specification rail `ol.spec` — one structural top rule, four territories (index label-tier 45%, fact functional-tier 68%, `34ch`; copy per the client, 2026-09-04: “Camera active only during a session. Nothing is stored.” / “No account, no registration, no face database.” / “Live stock is checked before every item is shown.” / “49-inch portrait display, readable from a natural standing distance.”), four columns ≥1280, 2 × 2 from 700, stacked below. No card, border, shadow, glow, backing panel, icon or badge. **Narrow:** label → headline → paragraph → product media (phones: 118vw wide, centred, ≤84svh; tablets: gutter-to-gutter) → rail. Video-ready: `<video class="object__media" autoplay muted playsinline loop poster>` replaces the `<img>` inside `.object` with no layout change; no scripted wake. Approved pending the photoshoot clip.
5. **Act two · In the business** — white; **Act one's system, mirrored**, from the chapter opening to the end of feature 03. Same markup shape, same tokens (`--media-w: clamp(720px, 60.5vw, 1440px)`, `--media-h: clamp(520px, 71svh, 940px)`, `--breath`, `--stick-top`), same `section-md` padding, same breakpoint (`min-width: 1100px and min-height: 600px`), same rail rhythm (`min-height: 80svh`, `padding-block: stick-top + 16svh / 10svh`, gap `clamp(96px, 12vw, 160px)` narrow), same active treatment (inactive block 0.35 → 1, 320ms), same trigger band (`top 72%` / `bottom 28%`, `onEnter`/`onEnterBack`) and the same 300ms crossfade with a 320ms leaving class. The single compositional difference: the media plane runs to the **left** viewport edge and the narrative rail sits on the **right** — only the sticky section is mirrored — the intro follows Act one unmirrored (grid `minmax(0, 1fr) var(--media-w)`, label and title left with the authored break “What it returns / to the business.” at the **display** tier, `white-space: nowrap`; the supporting statement right in column 2 row 2). The rail copy uses the shared annotation system (§5 of the floor chapter): index only, body-tier title at 500, small-tier description and impact sentence in ink, mono label, one left axis, `34ch`. `--breath` here is tighter than Act one's — `clamp(32px, 3vw, 56px)` (43px @1440) — so the annotation sits close to the image. The intro reads left to right as one unit: the title block takes its natural width (`max-content`, `white-space: nowrap`, authored break) with the supporting statement beside it, baselines aligned. Narrow: no stage and no script — image (4:5 → 4:3 ≥640 → 16:10 ≥900), index + category, headline, description, retail impact. The estate visual is the one asset-driven exception: `contain` on its own ground `#141211` in both layouts so the store list is never cropped. Verified against Act one at 1100/1440/1920: identical media size (871×639 @1440), identical stick-top, gutter and gap, mirrored sides.
6. **The console** — white; `section-md`; object-led and on **one row** from 1024px up: the editorial cluster (mono eyebrow, section-tier statement with its authored break, `--type-small` paragraph) in a `clamp(340px, 26vw, 480px)` column at the page gutter, and the intact console object beside it taking everything left over and running to the right gutter, the two levelled with `align-items: center`. Everything the console knows stays inside the object — the ESTATE OVERVIEW / ILLUSTRATIVE bar, both metrics with deltas, the store rows and status pills — with SAMPLE DATA · NOT A LIVE ESTATE beneath. Sharp geometry, one hairline border, no shadow, no new scroll behaviour. Narrow: cluster, then the object at full content width.
7. **Handoff** — the physical reset: the photograph **full-bleed** (`100vw` via `margin-inline: calc(50% - 50vw)`), `clamp(420px, 70vh, 900px)` tall, cropped at 52%/42% with the parallax kept; 4:5 below 640px. `padding-block: 0`, so the answers chapter's own top padding is the transition after it. Sequence: console/system → immersive store image → buyer questions.
8. **Before you commit** — white; eyebrow `FAQ`, section-tier heading “Before you commit.”, six answers, all open, numbered, sticky heading (grid 5/12 · 7/12 so “The questions / a buyer asks.” holds two lines at statement scale). Frozen otherwise.
9. **See it in your store** — black; display title, magnetic "Write to us" button, printed + copyable address, note. Frozen.
10. **Footer** — black; `display-vagora-only.svg` full width, `© 2026 Vagora`, email, "Site by Aqib". Frozen.

**Dock** (only navigation) — frosted ink pill fixed bottom-centre: brand mark (33px tall, 21px ≤460px; reloads the page; no hover wash, no active state, subtle 24° turn on hover) · 1px divider · Experience / Business / Contact with the text-flip hover (twin label rises from below; not on the active link) and a translucent wash for the active link. Proportions approved in-browser and applied as bounded fluid values, one source of truth on `.dock`: `--dock-link-h: clamp(48px, 46px + 0.45vw, 52px)` (48 phone → ~49 tablet → 52 from ~1333px, never larger), `--dock-type: clamp(15px, 14.6px + 0.1vw, 16px)`, `--dock-link-pad: clamp(10px, 4px + 1.1vw, 20px)`; `gap: 0` and `padding: 4px` at every width; `max-inline-size: calc(100% − 24px)`. Measured: 313×56 at 360/390, 349×57 at 768, 401×60 at 1440, 402×60 at 2000+. `--dock-h` = link height + 8px; `--dock-inset: calc(max(1vw, 16px) + env(safe-area-inset-bottom))` — approved in-browser at 1vw on a 2560px-wide desktop, 16px floor on anything narrower than 1600px (the footer clears both). Scrollspy by viewport-middle coverage; nothing lit over the intro. **Frozen.**

**Cursor** — 12px white dot, `mix-blend-mode: difference`, grows to a filled 44px disc over links; fine pointers only; re-shows on every pointer move (fixes vanishing after blur). Frozen.

## 5. The Experience chapter (What happens on the floor) — current spec

Markup: `.floor` → `.floor__head` (eyebrow, `What happens<br>on the floor.` display title, lede) → `.stage` → `ol.stage__steps` with five `li.step` (each: `figure.step__media` + `div.step__block` with `(0n)` index, `h3.step__title`, `p.step__body` and a Retail impact block) and `.stage__media > figure.aperture` holding the five `img.aperture__state`.

**Wide layout** (`min-width: 1100px` and `min-height: 600px`):
- Two phases. Phase A: head on the stage grid — title left (may run past its column), lede in the second column with its left edge on the media's left edge, media entering below-right. Phase B: narrative rail (remaining width) · breathing gap `clamp(48px, 5vw, 128px)` · media plane `60.5vw × clamp(520px, 71svh, 940px)` flush to the right viewport edge, sharp, sticky and vertically centred. Steps are 80svh each; the first step waits `stick-top + 16svh` below so State 01 reaches its reading band only after the head has left the viewport.
- State machine (`floor()` in `main.js`, registered via `gsap.matchMedia` on the same query): a step activates when the top of its reading block crosses 72% of the viewport (`start: "top 72%", end: "bottom 28%"`, `onEnter`/`onEnterBack`); between blocks the last state holds; inactive copy at 35%. All five stage images are set to eager when the stage is within 1.5 viewports. The change of state itself belongs to `createMediaTransition()` (`src/modules/refraction.js`) — a directional wave of refraction, 550ms, entering from the edge the content is travelling towards (items 52–53). The plane's entrance is also the engine's: the frame is never moved, the picture is revealed inside it from the bottom up. The trigger bands, the sticky mechanics and the layout are untouched by both. **Act two runs the identical machine, mirrored** (`stickyStage()` serves both).
- Step headlines may run into the breathing gap (`width: 100% + breath − 8px`) so `The garment moves / when they move.` holds two lines at 1440 and 2000.

**Narrow layout** (everything else): no sticky media, no script. Each state is one scene in normal flow — image (4:5 <640, 4:3 <900, 16:10 otherwise; full content width, `object-position` per image) → index + category → headline → description — with `clamp(96px, 12vw, 160px)` between states. The head is stacked and State 01's image is the introductory image. The unused image set in either mode is `display: none` + lazy, so nothing loads twice.

No local progress UI. Retail impact is withheld from this chapter (customer story here; retailer story in Business).

**Feature annotation (shared with Act two):** index only — `(01)` in Fragment Mono at the label tier, 400, light tracking — then 12 → title at the **body** tier weight 500, line-height 1.25, ink → 16 → description at the **small** tier, 400, ink → `clamp(32px, 1.9vw, 48px)` → mono RETAIL IMPACT label → 8 → impact sentence at the small tier, ink. No rule between the description and the label. On wide layouts the block is the annotation column itself — `--annot-w: clamp(280px, 16.5vw, 320px)` (280 @1440, 317 @1920, 320 @2560) — with no child wider than it; narrow layouts keep the 34ch measure. One exact left axis in both acts. Wrapping: titles `text-wrap: balance`, descriptions and impact sentences `text-wrap: pretty`; no manual breaks and no non-breaking pairs are needed — a last-line orphan check across 2560/1920/1440/1100/390 returns none. The annotation sits against the image: `--breath: clamp(24px, 1.25vw, 32px)` (24px normal desktop, 32px @2560), with Act one's block pushed to the end of its rail (`margin-left: auto`) so both acts show the same image-to-copy relationship; narrow layouts keep 24px between image and copy. The head keeps its own `--head-gap`. Act one's Retail impact lines are now shown. Chapter ledes are ink, not grey.

**Pending for this chapter:** tablet/mobile fine-tuning.

## 6. History of passes (newest last)

1. New art direction + Vite project (serif, warm palette, WebGL hero lens, top nav) — superseded.
2. Client revisions: Switzer only, v5 palette, no shader, bottom dock back, film-only hero, no eyebrow rule, footer wordmark SVG, no renders line.
3. Dock: divider, text-flip hover, brand cell exempt; mark −10%; hover turn.
4. Index `(01)` beside eyebrows; authored `<br>` in "What happens / on the floor." and "See it in / your store."
5. Cursor: blur-recovery fix; difference blend and filled disc kept.
6. Floor rebuilt from horizontal carousel to vertical sticky scrollytelling; pass 02 (mirror aperture, 12-col grid) → reverted to neutral by request.
7. Floor static composition: 62vw edge-bleed stage, annotation rail.
8. Foundation refactor 01 (five tiers, monochrome, page gutter) and 02 (six tiers, functional copy, tighter gutter, larger device/hover image caps).
9. Floor opening → State 01 two-phase choreography; stage 60.5vw; `[hidden]` fix so Retail impact truly disappears.
10. Floor responsive: stacked scenes below 1100px; 72% activation band; 300ms swap.
11. Mirror chapter → light product study (superseded by 12): `#F7F7F6`, no CSS hardware/glare/tilt, swappable `.object` media container. Scripted wake added then removed (a real start-up clip will replace it). Naming corrected: the chapter is THE VAGORA MIRROR (not an act); the object moves to the left, information right; container designed for the complete product photograph. Spatial refinement: bounded media–information gap (72–120px) replacing the 6fr/6fr split that drifted apart on large screens. Media swapped to the full-product photograph `vagora-mirrror.jpeg`, UI overlay removed.
12. Mirror chapter → dark sculptural product chapter: `#0F0F0F`, `vagora-mirror-dark.jpeg` as a height-driven product field with an edge mask, editorial annotation right, technical specification rail beneath, 1680px local stage, text-first stack below 1200px.
13. Dock refinement passes (compact control surface, sliding capsule, then hairline marker) — applied and then **reverted in full** at the client's request: the dock is back to its pass-3 form (frosted pill, 56px links, translucent active wash, coverage scrollspy). The reverted passes remain in the session record only.
14. Dock proportions: manually approved desktop values (52px links, 16px labels, 0 gap, 4px padding) applied as bounded fluid clamps; the old `gap: 4px` / `padding: 6px` / 56px links / ≤460px padding override removed.
15. Act two art direction: feature ledger + sticky evidence stage replacing the hover-image list; console enlarged to a 32/60 operating-surface composition; handoff photograph cleaned of overlay notes; Act two/console/handoff rhythm tightened (`section-lg`/`section-md`, handoff `padding-block: 0`).
16. Act two ledger: shared sticky/switching evidence stage removed (markup, CSS, `ledger()` JS); each feature row carries its own evidence in a 25/29/40 band on wide screens, linear unit below 1024px.
17. Act two heading hierarchy: two local clamps (superseded by 18).
18. Site-wide type rebuild: display / statement / body / small / label tokens replace the six-tier set; `--type-large` and all local heading clamps removed; Act two chapter title promoted to display, feature and console headlines to statement, FAQ questions to body + heading weight; Act two ledger and console two-column layouts start at 1200px.
19. Typography polish: split-line mask given a glyph safety area and released at rest (fixes the cropped descenders/terminals: “buyer”, “happens”); heading weights 600 → 500 light, 450 dark; reveal start offset 120%. Verified by pixel-diffing every display/statement heading against an unclipped render at 390–2560.
20. Fluid curve correction: display `1.5rem + 7.5vw` and statement `1.5rem + 3.33vw` (132 / 72px at 1440, caps 160 / 84); wrapping solved by composition width only — business head 8/12, Act two band 30/26/38 from 1280px, answers grid 5/7.
21. Fragment Mono micro type system: self-hosted regular weight, `--mono` token; every eyebrow plus 22 `.notation` elements (indexes, classifications, RETAIL IMPACT, specification and FAQ indexes, console metadata) in mono at one 14px tier; headlines, prose, questions and answers, dock, buttons, links, metrics, store rows and the manifesto's three meta items stay Switzer.
22. Act two + console art direction (superseded by 23).
23. Act two + console rebuild (superseded by 24).
24. Act two → the second sticky chapter (superseded by 25).
25. Act two refactored onto Act one's exact system — same tokens, spacing, breakpoint, rail rhythm, trigger band, crossfade and narrow fallback — mirrored so the media plane runs to the left edge and the rail sits right. The console was not touched in that pass.
26. Act two/console refinement: the intro un-mirrored onto Act one's exactly (display-tier heading, title left, statement right); feature copy dropped to body/small/label tiers as compact annotation on one left axis with the title offset removed; the console made still — grouped text block left, one object right, sticky removed.
27. Editorial contrast pass across both acts: feature eyebrows reduced to the index alone; one shared annotation system (body-tier 500 title, small-tier ink description and impact, mono index and label) replacing statement-scale feature headings; chapter copy set in ink rather than grey; Act two's breath tightened and its intro paired heading + statement on one baseline.
28. Feature copy and spacing unified across both acts: new titles, descriptions and impact lines (client copy), Act one's impacts shown, the description/impact rule removed, description → RETAIL IMPACT `clamp(32px, 1.9vw, 48px)`, image ↔ copy `clamp(24px, 1.25vw, 32px)` in both. Fixed a latent bug in both state machines: a rapid sequence of changes could leave `is-leaving` stuck on the active image and blank the stage.
29. Annotation measure: the feature copy column fixed at `clamp(280px, 16.5vw, 320px)` and anchored `--breath` from the image in both acts (320 / 32 at 2560), with balance/pretty wrapping.
30. Final type roles: display 160 / lead 84 / chapter 64 / section 48 / feature 22 / copy 17 / micro 14. Act openings moved from display to the new chapter tier; `.h2`/`.h3` to the section tier; the Mirror paragraph, FAQ answers and console paragraph to the small tier; FAQ copy is now `FAQ` + “Before you commit.”; the console recomposed as a small intro above a large offset object.
31. One eyebrow → heading distance, site-wide: `--eyebrow-gap` (24px) on `.eyebrow`, all per-section overrides removed, the ask's inline `margin-top: 28px` dropped. Two invisible causes of the drift fixed as well — the split mask's negative safety margins were collapsing out of headings in block parents (the FAQ eyebrow was overlapping its heading by 4px), now contained with `display: flow-root` on `.split-ready`; and each tier's own top leading is taken back with `--eyebrow-lift` (display, lead, chapter; the section tier is the reference). The *seen* gap now measures 32–36px at every tier with ≤1.8px spread at 390 / 1024 / 1440 / 2560, instead of the previous −4 → +28px.
32. One colour for the whole mono voice: every 14px Fragment Mono element now takes the surface's secondary tone through `.eyebrow` and `.notation` — 62% ink on light, 68% white on dark — resolved by the surface scope rather than declared per element. The six local `--text-3` declarations (`.spec__index`, `.impact__label`, `.surface__tag`, `.metric__label`, `.console__note`, the FAQ index) are gone, so eyebrows, indexes, RETAIL IMPACT labels and console metadata no longer sit at two different weights of grey. The 45% muted tone is reserved for a third level that spacing and position genuinely cannot carry; no notation uses it.
33. One tracking for the whole mono voice: 0.02em on every 14px Fragment Mono element. The three values it had — 0.055em on eyebrows, 0.045em on `.notation`, a 0.055em uppercase override on four labels, plus a dead `--ls-label: 0.06em` on `.spec__index` — are collapsed into a single declaration each on `.eyebrow` and `.notation`; the `--ls-label` token is removed, since the mono voice owns its own tracking.
34. Mono colour reduced to one decision: `--notation` — `rgba(15,15,15,.55)` on light, `rgba(255,255,255,.52)` on dark — resolved by the surface scope and carried by `.eyebrow`, `.notation` and the two feature indexes. No mono element is lighter or darker than another any more.
35. Two surfaces only. The Vagora Mirror's `--bg: var(--ink)` override is gone, so the chapter sits on the site's one dark surface, `#000000`; the unused `[data-surface="object"]` scope and its `--surface-object: #f7f7f6` token are deleted. Every painted section background now computes to `#FFFFFF` or `#000000`. `#0f0f0f` survives only as ink — `--ink`, `--text-primary` and their alphas (rules, media ground, the dock's frosted wash, `--control-bg` on filled controls), which are text/chrome, not surfaces. `.entry__media--ui` / `.evid__state--ui` keep `#141211`: that is the console screenshot's own ground, matched so a contained asset is not letterboxed against a seam.
36. **Motion pass, then triage.** A full cinematic system was built (WebGL refraction transitions on both acts, chapter-wide colour takeovers, crop expansions on the large images, a single FAQ entrance, hero pointer drift) and then **rejected in use**: the takeovers covered live text, the crop expansions read as broken photographs correcting themselves, and the intro made the reader wait. Reverted to the pre-pass score; `MOTION.md` records what survived and why.
37. **Kept from that pass**: the preloader tagline (below); the dock inverting on dark chapters; the Mirror product resolving by exposure with the spec rail arriving after it; the console arriving as one plane out of perspective, with the count-ups gone and the sample values written into the markup (they used to render as `0` with JavaScript off); the store photograph's crop expansion and drift; the wordmark rising inside a fixed mask; the manifesto's three facts waiting for the proposition; the quieter 10px/700ms reveal and 0.035 heading stagger; pointer depth on the product and the console; and the narrow-screen crop expansions.
38. **The preloader** now carries the tagline **“Beyond the Mirror”** instead of a 00–100 count, set to the full viewport width between small margins and fitted by measurement rather than a guessed clamp. Letters rise out of the bottom edge left to right; the line then travels upward *with* the retracting black, on the same start and curve. Scrolling is handed back when the black clears — the page no longer waits on the film's scale tween, which used to block it for a further second.
39. **The hero's black interruption is in the asset**: full exposure to 10.833s, then a baked fade to black over the last ~0.95s. Native loop off; playback restarts at the last clean frame, frame-accurately via `requestVideoFrameCallback`. A re-export without the fade is the real fix.
40. **No WebGL on the page.** `src/modules/gl-media.js` was deleted with the sweep.
41. **Preloader exit — carried, not staged.** A staged version (tagline out, mark out, clean black, then the lift) was built and rejected: it broke the exit into disconnected moves and left an empty black slab. The exit is now ONE gesture — the panel lifts and the phrase and mark ride it upward at full opacity. The phrase travels slightly faster than the panel's bottom edge from the first frame so its ink is never on the hero side of the boundary, then slips further ahead in the last fifth to cross the top edge in 75–85ms; the remaining clean edge finishes 120–150ms behind it. Measured per frame at 2560/1440/390: worst edge overhang −70.5/−41.1/−17.3px (always inside the black), minimum opacity 1 while visible, traverse 83/75/18ms, trailing black 125/151/117ms.
   The lift eases on `power1.inOut`, not `power2.inOut`: a cubic in-out spends a fifth of its duration on the last few per cent of travel, which was what stranded the empty panel — switching curve cut the trailing black from ~190ms to ~130ms without touching the choreography.
42. **Optical bottom.** The tagline is now placed by measured **ink**, not by margin: `fitLine()` reads the descender through the font's own metrics and sets `bottom` from it, giving 12px of visible black beneath the "y" at 2560, 8px at 1440 and 9px at 390 (measured from the rendered pixels, not from the CSS). Line-height 0.84; the mask's bottom padding now only prevents clipping and has no visual cost.
43. **The preloader mark** is one clamp instead of a fixed 34px: `clamp(42px, 40.5px + 0.37vw, 50px)` — 42px on a phone, 45.8px on a normal desktop, 50px at 2560 — with `aspect-ratio` carrying the height so the artwork can never distort. It reads as a brand mark rather than an interface icon, and stays below the tagline in the hierarchy. It is also **optically centred rather than geometrically**: "Beyond the Mirror" is a large mass along the bottom edge, so a mark at exactly 50% reads as low. It is lifted by `clamp(9px, 3vw - 3.2px, 56px)` — measured at 9px on a phone, 40px at 1440, 48px at 1707, 56px at 2560, putting its centre at 45.0–45.9% of viewport height on desktop and 48.9% on a phone.
   The lift is applied as `margin-bottom` (twice the lift, since centring aligns the *margin* box), **not** a transform: the exit animation owns the mark's transform, and a translate here would be silently overwritten the moment the exit starts. Horizontal centring is exact at every width.
44. **Hero → manifesto is now a chapter change, built in CSS.** `.hero-scope` is 200svh, the hero is `position: sticky` inside it, and `.manifesto` is pulled up by `calc(100svh - var(--hero-hold))` and painted above — so the white surface rises from the bottom edge over a held, full-bleed, still-playing hero. Net page height is unchanged, nothing is trapped, and the handoff works with JavaScript off. On wide viewports the film answers with a scrubbed `scale 1 → 1.025` and `brightness(1 → 0.88)`; narrow screens keep the composition and drop the filter. Measured at 1440 and 390: white rises monotonically 0 → 100%, hero held at `top: 0` and exactly viewport-height throughout, dim bounded at 0.895.
45. **The manifesto now answers the white**, not the hero — its scrub starts at `top 45%` instead of `top 78%`, so the first word lights at **79% coverage** rather than while the film is still most of the screen.
46. **Hero entrance reduced to a settle**: the preloader's reveal IS the entrance, so the film's scale tween is 1.015 → 1 over 1.2s (was 1.08 → 1 over 1.9s).
47. **The film's loop is crossfaded**, media to media, with a second decoder — 550ms, wide viewports only. Only the outgoing reel fades, over an incoming one already opaque: fading both lets the black behind show through and dips the hero ~25% at the midpoint. Re-confirmed by diagnosis that the film has **no internal dark cuts** (luma 113–133 across all 356 frames) and that the black is the asset's baked fade tail — still to be fixed by a re-export.
48. **Dock tone now hit-tests** rather than reading DOM boxes. During the handoff the hero's box still spans the viewport while the white is painted over it, so a box test kept the pill inverted — white on white. `elementFromPoint` at the far left reads the surface actually on top; the box test remains as a fallback.
49. **`boot()` guards `film()`.** A single undefined constant there aborted boot and left the whole page unsplit, unrevealed and unscrolled — one broken part must never take the page down with it.
50. **The dock has one state again.** The inverted (light) pill and the tone-tracking logic are removed: recolouring at every section handoff drew more attention than a persistent navigation should. Verified across 41 samples of a full-page scroll — exactly one dock appearance, `rgba(15,15,15,0.88)` with white labels, everywhere. This supersedes items 37, 40 and 48.
51. **Manifesto → Act one is an authored handoff**, not a surface takeover — both sides are white, so what changes is which thing owns the page. Four overlapping beats in `chapterHandoff()`: the proposition rises and recedes to 45% without leaving; the three facts follow it, later and less far; the chapter marker arrives while the proposition is still yielding; the large image rises and resolves into the stage, and only then does the first annotation settle. Position and opacity only — no clip, because a crop opening on an image this large is what read as a broken photograph correcting itself in item 36. The annotation moves on transform alone; its 0.35 → 1 opacity belongs to the sticky state machine and two systems must never share a property. Measured at 1440: yield and marker begin on the same sample, meta two later, image fully present two samples before the annotation finishes.
52. **Act one's plane re-renders.** The five states no longer cross-fade. `src/modules/refraction.js` puts one WebGL1 canvas over the sticky plane and passes a wave of refraction through the frame — a narrow gaussian band riding the transition front, displacement signed across it like a cylindrical lens, the front bowed off a straight line by two low harmonics, and a 3% light lift where the glass catches. Hand-written, not Three.js: a scene graph for one textured quad would outweigh the site's whole script.
    - **Directional**, from which way the annotation's trigger was crossed rather than from velocity: down-scroll the wave falls from the top, up-scroll it rises from the bottom. Proven by reading a recording frame by frame — the incoming state fills from the correct edge in every sampled frame of both directions, as a single moving boundary in 100% of frames.
    - **No idle state.** Everything is scaled by `sin(pi · p)`, so both ends of the crossing are exactly the source pixels, and the canvas is not composited between transitions at all — the `<img>` is what is on screen. Measured: 0.000/255 over 600ms at rest; the settled frame is 0.00/255 from the plain photograph; every state still fills the plane exactly, so nothing about the crop or sizing moved.
    - **550ms measured** over 18 animation frames on a software rasteriser. Velocity shades it only: up to +30% displacement, down to 460ms.
    - **Text follows the image**: the outgoing annotation recedes in 200ms, the incoming one waits 260ms and then arrives. Measured 33ms and 286ms — 253ms apart.
    - **Robustness**: one canvas, five textures uploaded once at about the size the frame is shown at, nothing allocated per frame, no rendering when settled or offscreen. Interruption retargets — a reversal swaps the ends and runs the same wave back from where it is; a third state mid-crossing either replaces what is arriving or becomes the next wave's origin, by which side of halfway it is. Hammering the rail leaves exactly one state painted and nothing running.
    - **Fallbacks.** WebGL missing, the context lost, or three crossings that could not hold ~24fps (median frame interval, so one stalled frame is not evidence) retires the canvas for the session; the same event is then authored in GSAP as a directional wipe from the same edge, resolving from 1.4% oversize, with the outgoing state solid underneath so no ground ever shows. Under `prefers-reduced-motion` there is neither — a state change is not an animation, and it happens in 200ms of CSS with no delay to wait behind. Narrow screens have no sticky plane and no engine.

53. **One motion system across both acts, and the entrance separated from the position.** Three changes, all inside acts one and two.
    - **The stage entrance was moving the wrong element.** `chapterHandoff()` tweened `[data-aperture]` up 56px into place while the browser's sticky layout was about to take control of the same position — so the borrowed distance had to be surrendered at the handover, and the plane read as sliding backwards to find its place. Measured on the approach at 1440: **seven samples of page-relative reversal**. The tween is gone. The frame is now established at its final sticky geometry from the first frame, and the entrance happens *inside* it: `media.reveal(progress)` clips the frame open from the bottom up while the state resolves down from 1.6% oversize, scrubbed on the same `top 90% → top 45%` window. Re-measured: **zero reversals**, in both acts. Position belongs to the browser; what is inside the frame belongs to the engine; nothing writes to both.
    - **The wave now travels with the page, not against it.** Scrolling down is content moving *up* through the viewport, so the wave rises from the bottom edge; scrolling up it falls from the top. One line in the shader (`axis = uDir > 0.0 ? vUv.y : 1.0 - vUv.y`) and the mirrored `inset()` in the authored fallback. Nothing else changed — same band, same strength, same 550ms, same mathematically-exact settled state. Verified from recordings in both acts and both directions: the incoming state fills from the correct edge in every sampled frame, as a single moving boundary in 100% of frames.
    - **Act two runs the same machine.** `floor()` and `ledger()` were near-identical state machines; they are now two four-line calls to `stickyStage()`, which owns the engine, the activation triggers, the entrance and the teardown. Both acts therefore share the entrance, the wave, the duration and easing, the direction rule, the 200ms-out/260ms-then-300ms-in annotation sequencing, the interruption and reversal handling, and the WebGL/clip/reduced-motion fallbacks by construction rather than by copy. The canvas class is `.media__gl` and the scripted-plane rules name both planes.
    - Act two's third state is an interface capture shown `contain` on its own `#141211` ground, which the shader's cover mapping could not express. Rather than teach the shader about fit, the texture is **composited onto that ground at the frame's aspect on upload**, so it fills the frame like any photograph and the glass bends the letterbox with everything else. Textures are re-uploaded when the frame's aspect changes, not only its width.
    - Measured after, act two: crossing 533ms over 20 animation frames; 0.000/255 of change over 600ms at rest; settled frame 0.00/255 from the plain photograph; annotation out at 134ms, in at 377ms (243ms apart); a hammered rail leaves exactly one state painted and nothing running; teardown leaves no canvas, no inline residue and no entrance clip. Reduced motion and narrow layouts build no engine in either act.

54. **The act one chapter marker was being animated twice.** `.floor__head` is in `CLAIMED`, but `CLAIMED` was only applied to the `.reveal` pass — the `.split-ready` heading pass took every heading, `#floor-h` included. So the quiet word rise started as the heading came into view (~83% of the viewport) and the authored `chapterHandoff()` entrance restarted the same words from hidden at 72%. Measured on the approach at 1440: mean word opacity climbed 0.19 → 0.52 → 0.74 → **0.86, fell back to 0.76**, then climbed again — it read as revealing, faltering, and revealing a second time. `CLAIMED` now filters the heading pass as well. Superseded in the same breath by item 55, which removed the authored entrance instead.

55. **Act one's chapter marker now arrives like every other heading.** Fixing the double-drive (item 54) left the two acts' heads still animating *differently*: `chapterHandoff()` authored act one's with its own trigger (`top 72%`), travel (`yPercent 115`), duration (0.95s) and stagger (0.03), while act two's — and every other section heading — came through the quiet pass (IntersectionObserver at `-12%`, `yPercent 120`, 1.15s, stagger 0.035). The markup of the two heads is identical, so the difference read as a mistake rather than as emphasis. The authored beat is deleted, `.floor__head` is out of `CLAIMED`, and the head goes through the same pass as everything else. Measured side by side at 1440 the two acts are now indistinguishable: rise begins at 761 / 763px from the viewport top; mean word opacity climbs 0.07 → 0.29 → 0.68 → 0.81 → 0.90 → 0.95 → 0.98 → 0.99 versus 0.06 → 0.30 → 0.61 → 0.78 → 0.88 → 0.94 → 0.97 → 0.99; peak stagger spread 0.55 vs 0.59; the eyebrow ramps identically and both ledes follow one `data-delay` beat behind. `chapterHandoff()` keeps only what is genuinely its own — the manifesto yielding and the annotation landing — and the marker still begins on the same sample as the yield, so the overlap the handoff was built around is intact. **Nothing on the page claims a heading now.**

56. **The manifesto is a framed composition, and act one waits for it.** Two changes, both about vertical composition rather than motion.
    - **The manifesto section had no height of its own** — it was content plus `--section-md` padding (748px in a 900px viewport, 564px in a 1180px one), so the group sat near the top of a white field with the next chapter filling the rest. It is now `min-height: 100svh` with `display: grid; align-content: center`, placing the eyebrow + lead line + three facts as ONE unit. `min-height`, never `height`: at 360×640 the composition is 718px and the section grows to 814px, keeping its padding rather than cramping. The frame padding (`clamp(48px, 7svh, 104px)`) is a floor that only decides anything once the composition outgrows the viewport.
    - **Internal spacing is untouched, and proved so.** Every element in the group carries an explicit margin (the reset zeroes h1–h3 and p), so switching the section to grid loses no collapsed margins. The suite strips the framing back off at runtime and re-measures: eyebrow→head and head→facts are identical in the frame and in plain flow at all seven viewports (22/86px at 1440, 23/61px at 1024, 23/48px on a phone).
    - **Placed optically, not mathematically:** `--manifesto-optic: clamp(6px, 1svh, 14px)` is subtracted from the top padding and added to the bottom, lifting the group 1% of the viewport above centre — so the two gaps differ by 2% of the viewport at every size (197/215 at 1440, 435/463 at 2560, 392/416 at 820×1180, 74/91 at 390×844). The value was chosen by rendering variants at 22/8/0/−14px and looking: the mass is the lead line, which sits in the upper half of the group, so a little more room below is what stops it reading as sinking, and more lift than this makes it float.
    - **Act one's opening now waits.** `reveals()` gained a second observer at a later threshold, selected by `data-enter="late"` on `.floor__head` — the same animation, asked for later. Before: the heading fired on first intersection, while the manifesto still held two thirds of the screen. After, measured at 1440/1024/390: zero pixels of act one are visible while the manifesto is framed, and when the heading begins act one owns 60–64% of the viewport with the head at about half the screen. Only act one is marked late; every other section is untouched.
    - Consequence, accepted: the proposition's lit sweep now begins at 88% white coverage on a desktop rather than 79%, because the lead line sits lower in a centred composition. The hero → manifesto handoff itself is unchanged — scope 1800px, margin −900px, coverage still rising 0 → 100% monotonically over a held, full-bleed hero.

57. **The manifesto is top-anchored, and the takeover carries it.** Item 56's centred, `min-height: 100svh` manifesto was rejected in use; this replaces it.
    - **Anchored into the corner of its own surface.** The composition sits top-left, framed by one number: the *optical* distance from the top of the white panel to the ink of "VAGORA IS" equals the page gutter to its left. Optical, not box — the eyebrow's line box carries 3px of leading above its capitals (measured from the rendered pixels), so `padding-block-start: calc(var(--page-x) - var(--manifesto-cap))` with `--manifesto-cap: 3px`. The corner measures square at all seven viewports: 44/44px at 2560, 32/32 at 1440, 30/30 at 1280, 27/27 at 1024, 25/25 at 820, 24/24 at 390 and 360.
    - **Content-driven height.** `min-height`, `display: grid` and `align-content: center` are gone; the section is as tall as what is in it (612px at 1440, 788px at 390). No pinning, no invented scroll.
    - **The takeover is now one gesture.** Because the first line sits a gutter from the panel's leading edge, it crests the viewport with the surface. The manifesto's eyebrow lost its `.reveal` — it is painted on the panel, not faded in on top of it — and the lead line is already present at 18% behind it. Measured through the rise at 1440/1024/390, at every step from 8% coverage up, the bare white above the first ink is exactly the corner inset (29 / 24 / 21px). The proposition's lit sweep now begins at 74% coverage (was 88% centred, 79% before that), still comfortably after the film has gone.
    - **Deliberate breathing room after the facts:** `--manifesto-breath: clamp(56px, 10.5svh, 200px)` plus act one's own `--section-md` top padding, giving 21.0svh at 2560, 25.0 at 1440, 24.9 at 1280, 23.0 at 1024, 18.6 at 820, 21.9 at 390, 25.5 at 360. Real layout spacing.
    - **The handoff is now two beats, not one.** `data-enter="late"` moved off `.floor__head` and onto the heading and the statement only, and the late threshold went from −42% to −52% (a very tall viewport put the head above the old line while the manifesto still held the composition). Act one's LABEL therefore arrives on the normal threshold and peeks into the bottom edge — 82% of the viewport at 1440, 80% at 1024 — while the heading stays back. Measured: when the heading begins, act one owns 72–76% of the viewport with the head at 38%, and it arrives 0.40–0.44 viewports after the label, so there is no dead interval.
    - Internal spacing, typography and copy are untouched; the hero → manifesto transition's own geometry is unchanged (scope 1800px, margin −900px, coverage rising 0 → 100% monotonically). `actone.js` was rewritten: its old assertion that *nothing* of act one may be on screen encoded the rejected direction and now contradicts the brief.

58. **Act one's opening now matches act two's, in composition and in timing.**
    - **Layout.** `.floor__head` was a two-row grid with the statement in row 2 of the *media* column — stranded low and beside the image rather than beside the heading. It is now act two's rule exactly: `grid-template-columns: max-content minmax(0, 1fr)`, one row, `align-items: end`, statement `margin-top: 0; padding-bottom: 0.4em`. Measured at 1440/1280/2560, the two heads are now identical: same 2×1 grid, statement on the heading's row with a 0px baseline offset and the same 64/72/128px gap right of it, same head height, same 48px to the media.
    - **The media rose with it.** Folding the statement into the intro row shortened the head by 87–94px, and the sticky plane rose by exactly that. The head-to-media gap is unchanged at 48px, `--stick-top`, the steps' padding and every feature trigger band are untouched, so the handoff tightened without any new offset being invented.
    - **Timing.** Item 57 had split the opening: the label on the normal threshold, the heading and statement on `data-enter="late"` at −52%. That is 40% of a viewport of scrolling between the eyebrow and the heading — the eyebrow sat alone for several scroll steps. `data-enter="late"` now sits on the WHOLE `.floor__head`, so all three share one threshold and the group behaves exactly as act two's does: the eyebrow leads only by its own height. Measured at three viewports in both acts: eyebrow → heading **0px, one scroll step**, statement 80–141px later on its own 80ms `data-delay`. The opening still waits for the chapter — act one owns 66% of the viewport when it fires and nothing appears while the manifesto holds the composition.
    - **Trade-off, accepted:** the label no longer peeks alone during the final manifesto state (item 57's "promise of what is next"). Grouping the opening is what removes the disconnection, and the two cannot both be true.
    - **Correction to item 57's figures.** The proposition's lit sweep was recorded there as starting at 74% coverage; walked finely it is **67%** (the 74% came from an 8%-per-sample walk landing late). The suite's coverage floor was the wrong rule anyway and now asserts the intent directly: the first word lights only once the whole statement is clear of the panel's bottom edge — 273px clear at 1440, 411px on a phone.

59. **The feature annotations are placed in their fields, not off the image.** Desktop only; the narrow layouts are untouched.
    - Both blocks were positioned relative to the image — act one's `margin-left: auto` put its right edge `--breath` (24–32px) from the plane, act two's sat at the start of its rail. A fixed offset from a huge photograph reads as a caption attached to it. First replaced with a calc-based centre-plus-bias, then (item 60) with plain structural centring.
    - Text alignment is untouched (`start`); `--annot-w` is untouched; the grid, the sticky mechanics and every trigger band are untouched.
    - Measured at six widths, the two acts come out pixel-identical mirrors — gutter side / block / image side: 448/320/308 at 2560, 249/317/155 at 1920, 198/280/120 at 1600, 163/280/94 at 1440, 129/280/67 at 1280. Bias 6.4–6.5% throughout.
    - At 1100 the field (352px) is barely wider than the block (280px), so the hold governs and the block sits essentially centred (34px / 38px). There is no composition to lean at that width; the previous fixed gap only looked like one.

60. **The feature annotations are centred in their fields, structurally.** Item 59's 6.5% optical bias toward the image is rejected; the block sits dead centre.
    - The calc-based margins are gone, and so are `--annot-bias` and `--annot-hold`. Instead the **rail's box is stretched across the column gap** — `.stage__steps { margin-right: calc(var(--breath) * -1) }` and `.ledger__rail { margin-left: calc(var(--breath) * -1) }` — so that it spans the actual whitespace region, page gutter to the image's edge. The block is then centred with `margin-inline: auto`, and that is the whole rule. A negative margin overflows a grid item without resizing its track, so `--media-w`, the sticky plane and every trigger band are untouched.
    - No translate nudge, no offset from the image, nothing to retune when the type or viewport changes: the region moves, the object stays in the middle of it.
    - Measured at six widths — gutter side / block / image side: 378/320/378 at 2560, 202/317/202 at 1920, 159/280/159 at 1600, 128/280/129 at 1440, 98/280/98 at 1280, 36/280/36 at 1100. Off centre by 0–1px everywhere, and the two acts are pixel-identical mirrors.
    - Text alignment (`start`), `--annot-w`, internal spacing, the vertical centring in each 80svh step, imagery, sticky behaviour, media transitions and the narrow layouts are all unchanged.

61. **The console is one row.** It was a small text cluster on columns 1–5 of a 12-column grid with the object dropped into row 2 on columns 4–12, offset right and pushed down `clamp(64px, 8vh, 128px)` so their edges would not align. In use that left most of a screen of nothing between them and read as two fragments of different spreads rather than one statement — measured at 1440, a 1132px section with the copy ending at y556 and the object not starting until y644, with the whole right half of the first screen empty.
    - Now `grid-template-columns: clamp(340px, 26vw, 480px) minmax(0, 1fr)` with `align-items: center`: the explanation at its own measure on the left at the page gutter, the object beside it taking the remainder and running to the right gutter, the two levelled against each other. The section is 761px at 1440 (was 1132), 669px at 1024, 844px at 2560.
    - The copy column grows with the page rather than sitting at a fixed 360px, because the heading is on the section tier — fixed, it held two lines at 1440 and broke to three by 2560. Measured 340 / 374 / 480px at 1024 / 1440 / 2560, with a two-line heading at every width.
    - The object's own entrance is untouched: it still rotates out of an offset plane and settles flat, scrubbed, and its resting transform is `matrix(1, 0, 0, 1, 0, 0)` — identical to what the old layout produced, checked side by side.
    - Below 1024 nothing changed: the stacked composition is exactly as it was.

63. **[SUPERSEDED by item 64 — deleted, nothing of it remains]** **Act one hands over to the Vagora Mirror.** The boundary was a white section scrolling into a black one. It is now one transformation with its origin in the sticky media plane: the experience collapsing back into the machine. Nothing outside the transition changed — act one's layout, typography, copy, sticky mechanics and feature transitions, the Mirror's static composition, the dock and every other section are untouched.
    - **The field is the plane.** A script-created fixed `#000` element (`.blackout`, z-index 50, below the dock) clipped to a rectangle taken from the plane's own geometry: left and width from its rect, top from `--stick-top`, height from its box, radius from its `border-radius`. Measured at 1440 and 1920, the field's left and right edges are the plane's to **0px**.
    - **Three beats over scroll that already existed** — `.mirror`, `top bottom+=30%` → `top top`: 1.3 screens, scrubbed, nothing pinned, no distance invented. The annotation recedes (the whole rail, 1 → 0.55, never the per-step opacity the sticky machine owns); the field fills the plane's frame **bottom-up**, the direction the act's own refraction travels, while sticky still holds the plane — the first **23%** of the window, which is where sticky actually releases, measured, not chosen; then the field expands to the screen over the next 57%, left / up / down (the plane already meets the right edge). One symmetric ease across both halves, so the still beat between them is the seam.
    - **The photograph is absorbed, not covered.** The instant the frame is solid the plane is hidden — under an opaque black, so there is nothing to see happen — which stops it reappearing above the field as it travels on up the page. Nothing is left floating on black.
    - **The hand-off.** Once the field is the whole screen its foot follows the Mirror chapter's own top edge up, so the chapter is exposed as it arrives rather than after it has arrived. Both surfaces are `#000`, so the edge is invisible; what it removes is a held black beat and a product that would otherwise finish revealing behind the field. Measured: the foot tracks the chapter's top edge to **0px** at all three viewports.
    - **The chapter's order is authored.** `mirror()` now runs from one trigger at `top 20%`: product (0.85s, from 4.5% oversize, no travel), then eyebrow +520ms, heading +630ms, statement +720ms, specification rail +1160ms as one layer. `.mirror__body` joins the claimed list so the quiet reveal pass no longer owns those elements; the heading still uses the page's one heading animation through the shared `revealWords()`, which the quiet pass now calls too — one implementation, so no heading can drift from the others.
    - **Reversible and safe.** Every value derives from progress and scroll-invariant geometry: walked down and back at 1440, 1920 and 390, **0px** of drift at every sample, no white returning once the field is open, act one handed back whole. Under `prefers-reduced-motion` and with JavaScript disabled the field is never created and the Mirror chapter renders complete. Jumping past the boundary by dock link leaves no field painted; jumping back restores the rail and the photograph.
    - **Narrow:** the same three beats with simpler geometry — no sticky plane to originate from, so a full-width field rises from the foot of the screen and hands over the same way. No pinning added.
    - **Suite:** `/tmp/takeover2.js` (geometry, direction, reversal, colour sampled from the render), `/tmp/mirrorseq.js` (the chapter's order at rAF resolution), `/tmp/blackout-safety.js` (reduced motion, no-JS, jumps). Two existing suites were corrected, not the site: `motion.js` measured the act one plane from wherever a full-page scroll left it (past the boundary, where an absorbed plane is the correct state), and `glass.js` sampled act two before Lenis had finished ~800px of residual momentum, which carried the rail a whole step past its first.

64. **Reverted: the black-expansion transition (item 63) and a refinement of the mobile image entry.** Both were rejected in use and both are gone; neither was refined further. Item 63 is kept above as the record of a direction that was tried, not as a description of the code — none of it exists any more, and its suites have been deleted with it.
    - **The mobile act one / act two image entry is exactly what it was before item 65.** One crop, 12% each side → 0, 1.15s, `power2.out`, asked for at `top 88%`, once, over `.step__media`, `.entry__media` and the Mirror's figure together. No proportion-derived amplitude, no inner push-in, no `contain` exception, no changed trigger. Verified against the old contract on nine images at 390 and 1024: 12.00% → 0.00%, scale 1.000, one trigger each, 1.15s, `power2.out`, once.
    - **The custom black-expansion transition is deleted, not disassembled.** `chapterBlackout()`, the `.blackout` element and stylesheet, the `.aperture.is-absorbed` rule and the rail's recede are all gone; nothing of it is kept. The Mirror's own reveal is the only thing that survives, retimed.

65. **Act one → the Vagora Mirror is the hero's takeover, used a second time.** The site now repeats one principle at both changes of world: **the held world stays, the next world arrives over it from below.** It is the same implementation, not a second one.
    - **Act one is held, whole.** `.floor` is `position: sticky`, so the act scrolls normally until its final composition is on the screen and then stops — the section, not the text or the image separately. Measured at 1440, 1920 and 390 across the whole hold: the section, the media plane and the last annotation each move **0px**, and both plane and annotation are fully on screen throughout (plane at 131/157px, annotation at 160/236px).
    - **The moment is arithmetic, not tuning.** The plane's own sticky run ends when the stage's box runs out beneath it, which is the same scroll position at which the act's foot reaches the foot of the screen. So the hold begins exactly at the resolved composition — measured, it begins as the black reaches the foot of the screen (890 of 900; 1068 of 1080; 834 of 844) and ends as it reaches the top (35; 42; 32).
    - **The black rises as one plane.** `.mirror` is pulled back up by exactly the scope's surplus and painted above the held act, exactly as the manifesto is over the hero. Full width at every sample, one direction only, never giving ground back; the rendered screen goes from 93% light to 0.3% and never brightens again.
    - **The anchor is the only scripted part.** The hero is one screen tall so `top: 0` holds it; act one is many screens tall, so the anchor is `screen − the act's own height` — not expressible in CSS about a content-sized element. `actHold()` measures it on load and on every refresh and writes `--act-hold` / `--act-anchor`; teardown removes them. No pinning library, no transforms, no per-frame work.
    - **Nothing is invented and nothing is trapped.** The hold is real space and the Mirror's pull cancels it exactly (900px + −900px); the document is 14465px at 1440, identical to before the hold existed. Both properties are zero until measured, so with JavaScript off the page is simply the page — hold 0, the Mirror following act one with a 0px gap, nothing covered. Under `prefers-reduced-motion` the takeover still works, because it is a composition.
    - **Reverse and recovery.** Walked down and back at three viewports: **0px** of drift at every sample, act one handed back uncovered with its plane at 131px. Jumping past the boundary by dock link and back leaves nothing stuck.
    - **The Mirror's content rides up inside the black**, so there is no empty-black wait: the product's reveal is asked for by its own frame as it comes up (about a third of the way into the rise, chapter top 627–752px) and is established before the black has the viewport; the words follow once it does. Measured: 0 samples of black screen without the product resolving, and the product always leads the words.
    - **Suite:** `/tmp/pinned.js` (the hold, the rise, the reverse, and a direct comparison proving the hero's and act one's takeovers are one implementation), `/tmp/takeover-safety.js` (no-JS, reduced motion, page length, jumps, teardown), `/tmp/entry-restored.js` (the reverted image entry).

66. **Final responsive and interaction QA — eight viewports, no redesign.** 360×800, 390×844, 768×1024, 1200×720, 1366×768, 1440×900, 1920×1080, 2560×1440, the complete page at each, measured rather than eyeballed. Six genuine faults found and fixed; nothing else was touched.
    - **`100vw` full-bleed → layout width.** `.handoff__frame` was `width: 100vw` pulled back by `margin-inline: calc(50% - 50vw)`. `100vw` is the viewport *including* a classic scrollbar, so on any browser that reserves gutter (Windows Chrome and Firefox) the frame was a dozen pixels wider than the page and `body { overflow-x: clip }` quietly ate the right edge, shifting the crop off centre. The section is already full width, so the frame is now simply `width: 100%` with no negative margins. Not reproducible headlessly (overlay scrollbars), but structurally eliminated: the frame now measures exactly `0…layoutWidth` at every viewport.
    - **`vh` where `svh` was meant.** The store frame's height (`70vh`), the FAQ head's sticky offset (`14vh`) and the preloader tagline's pre-measurement fallback (`1.5vh`) were the only three viewport heights on the page not written in `svh`. All three now match the rest.
    - **`text-wrap: pretty` completed.** Present on the feature bodies, impact lines, console lede and FAQ answers; missing from `.lede` (both act openings' statements), `.mirror__lede` and `.spec__fact`. Added. `balance` on the headings is unchanged, and the authored break in *What it returns / to the business.* is untouched.
    - **Below-the-fold imagery was fetched during the intro.** `createMediaTransition()` promoted every state to `loading="eager"` and called `decode()` on all of them *at construction* — which happens at boot for **both** acts, so act two's three states (eight screens down, ~230KB) were downloading while the hero film, which is the first viewport, was still loading. The stage's own preload trigger was dead code behind it. The decode is now `prime()`, asked for by that trigger. Measured: 7 images fetched during the intro instead of 10, act two's three still lazy when the intro ends.
    - **…and the lead had to be two and a half screens, not one and a half.** At 150% the engine initialised close enough to the reader's arrival that a software rasteriser demoted it under the stress suite — the texture upload landed beside the first crossing. At 250% act one primes at the top of the page exactly as it always did, and act two primes somewhere in act one: still far enough back to stay out of the first paint, far enough forward to be warm on arrival. `glass`/`wave` pass in both acts and both directions again.
    - **`fetchpriority="low"`** on the two images the markup still declares eager (act one's first state, the Mirror's product), so neither competes with the film for the first viewport's bandwidth.
    - **Verified clean at all eight viewports:** no horizontal overflow at any scroll position; no text clipped by its own box; nothing left unrevealed after reading the page; no console errors; the type tiers scaling as authored (manifesto 42→84, act intros 36→64, section 28→48, feature 18→22, body 16→17, Fragment Mono flat at 14). Section flow has no gaps or unintended overlaps — the three overlaps present are the two takeovers and a parent/child pair.
    - **Short desktops (1200×720, 1366×768).** Both take the wide score. Act one's held composition fits entirely on the screen at both: plane 92…612 and annotation 105…398 of 720; plane 100…645 and annotation 102…421 of 768. The Mirror chapter is 1.54 and 1.37 screens tall. No fallback was needed and none was forced.
    - **The dock.** Its resting-state footprint is clear at every viewport at the page bottom, during act one's hold, at the Mirror's settled position and at the contact section. At the page's end the footer's last row ends 24px above it and the wordmark 98–136px above it. Content passing *behind* it while scrolling is inherent to a fixed element and is not a rest state.
    - **The preloader,** measured from the rendered pixels at all eight viewports (the box carries deliberate padding below the ink, so only the render can answer this): the descender's tail sits **8–12px** off the bottom edge and is never cut; the phrase is set to **92–97%** of the screen; nothing is clipped at the top or either side; the phrase clears the top before the black finishes, every time; the curtain is removed and the hero revealed clean. Cached reloads behave identically. The mark's optical lift is 9 / 20 / 33 / 38 / 40 / 55 px at 360 / 768 / 1200 / 1366 / 1440 / 1920 — the authored `clamp(9px, 3vw − 3.2px, 56px)`.
    - **The two act openings are one grammar,** measured at 1366 and 390: eyebrow and heading arrive together (heading 17–26ms *before* the label's own transition resolves) and the statement 97–115ms after, in both acts.
    - **Resize and orientation.** Driven through 1440×900 → 768×1024 → 1024×768 → 390×844 → 844×390 (landscape) → 1440×900: no overflow at any size, the act one hold and its anchor re-measured at each, and the page length and trigger count restored exactly on return.
    - **Reduced motion and no script**, at 360, 1366 and 2560: every element at its final state, no Lenis, no WebGL, no curtain, the film not autoplaying, no overflow. With no script the hold is 0, the Mirror follows act one with a 0px gap, and nothing is covered.
    - **Performance.** The refraction canvas caps device pixel ratio at 1.75 on a dpr-2 display; one canvas per act; 24 ScrollTriggers; 2 of 19 images declared eager in the markup; teardown releases triggers, canvases, Lenis and the hold's custom properties.
    - **Left unchanged, deliberately:** at 2560×1440 only, two half-screen intervals where the screen is still while a feature is being read (`.step { min-height: 80svh }` = 1152px there). Finely sampled every 120px, the longest still interval is **0px** at 1440×900, 1366×768 and 390×844 — this is the sticky stage's reading rhythm at a very tall viewport, not dead scroll, and capping it would move approved trigger positions.
    - **Suites:** `/tmp/qa-sweep.js` (all eight viewports, whole page), `/tmp/qa-detail.js` (resting states, vertical fit, widows), `/tmp/qa-motion.js` (openings, resize, fallbacks, performance), `/tmp/preloader-rest.js` (the preloader from pixels), `/tmp/deadscroll.js`, `/tmp/scrollbar.js`, `/tmp/closing.js`.

67. **The hero plays the new film.** `hero-video.mp4`, supplied by the client, replaces `vagora-intro`. Nothing else about the hero changed — full-bleed, 100svh, sticky under the manifesto's takeover, same preloader, same controls.
    - **Encoded for the web.** The source is 1280×720, H.264 at 7.5 Mbps, 8.000s, 24fps, with an audio track. As delivered it was **7.22MB** in front of the first viewport. Re-encoded, audio stripped, faststart: **709KB** mp4 (x264 veryslow, CRF 21) and **294KB** VP9 webm (CRF 30) — SSIM 0.995 against the source, and at 2× magnification the two are indistinguishable. The old pair was 1.4MB + 1.4MB, so the hero is now lighter than it was as well as newer. A new `hero-poster.webp` (6.9KB) is the film's own first frame, so the poster and the first painted frame are the same picture; it is preloaded in the head as the old one was.
    - **The loop had to stop being a constant.** `FADE_STARTS = 10.833` was matched to the old asset's baked fade tail. Against an 8s film that time never arrives — so the hero would have played once and **held its last frame for ever**, at every viewport. The loop point is now read from the media (`filmEnd()`), and the point taken is the last frame the film *presents*: a frame's `mediaTime` is the moment it appears, so a 24fps clip's final frame lands at duration − 1/24 and a test against the duration alone can only ever be false — which is exactly how the phone path still froze after the first fix. An `ended` listener is the backstop. Verified looping at all eight viewports, on both the single-decoder and the two-decoder path.
    - **The crop is keyed to aspect ratio, not width,** because what survives of a 16:9 film in a full-screen hero depends on the shape of the screen: 100% at 16:9, 42% on a portrait tablet, 25% on a phone. The subject decides the anchor — the Mirror occupies the frame's 19–42%, the customer stops at 50–63%. Below 0.62 the slice holds **the Mirror whole** (product, with the render on its screen, customer entering at the edge); to 1.2 it is anchored at 41%, wide enough for both; above that, centred. Measured at all eight viewports: the film fills the hero exactly, and the subject is 12.8–33.3% of the screen everywhere. Candidate crops were rendered and compared before choosing; a letterboxed "band on its own ground" was tried and rejected — the studio has a horizontal gradient, so a flat fill seams visibly.
    - **The film toggle needed more wash.** The new film is a white studio (mean luma 200–215) where the old one was 113–133. At the dock's shared `--frost` (`rgba(15,15,15,0.35)`) the control measured **2.4–3.0:1** against the studio — under the 3:1 a non-text control needs. It now carries its own `rgba(15,15,15,0.55)`: **4.3–5.0:1** measured at three viewports, with the white glyph inside at 4.8:1. The dock's token is untouched and the dock itself measures 14.35:1 over the new film, unchanged.
    - **Re-verified after the swap:** the full eight-viewport sweep (no overflow, no clipped text, everything reveals, no console errors), the preloader from pixels at all eight (8–12px descender gap, 92–97% measure), reduced motion and no-JS, resize and orientation, the performance surface, and every motion suite.
    - **Left for the client:** the source is only 1280×720, so at 2560×1440 it is upscaled 2× and reads softer than the rest of the page — a higher-resolution master would be worth having. `vagora-intro.webm`, `vagora-intro.mp4` and `vagora-intro-poster.webp` are no longer referenced and can be deleted.

## 7. Validation routine## 7. Validation routine

Build must pass (`npm run build`). Check 390×844, 768×1024, 1024×768, 1440×900, 2000×1125: no horizontal overflow, no glyph clipping on any display/statement heading (`/tmp/clip.js` pixel-diffs each heading against an overflow-visible render) (`scrollWidth === innerWidth`), no console errors, floor mode correct per viewport, activation at ~72%, reverse scroll restores earlier state. Also: `prefers-reduced-motion` (no Lenis/curtain/canvas, poster hero, all states rendered), JavaScript disabled (page complete, floor stacked), keyboard order and visible focus, `window.__vagoraDestroy()` tears down Lenis and all ScrollTriggers. Only one smooth-scroll engine (Lenis) is installed; no Three.js. The two WebGL surfaces are the acts' planes, created and disposed with their `matchMedia` contexts.
